<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_secondary"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.studios.hell.scrambledsalad.SecondaryActivity">
    <ImageView
        android:layout_width="50sp"
        android:layout_height="50sp"
        android:scaleType="fitXY"
        android:onClick="changeFolder"
        android:src="@drawable/folder"
        android:id="@+id/folderImage" />

    <ImageView
        android:layout_width="250sp"
        android:layout_height="250sp"
        android:scaleType="fitXY"
        android:src="@drawable/scrambled_salad"
        android:id="@+id/ScrambledSalad"
        android:layout_centerInParent="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="scrambleOrToss"
        android:id="@+id/scramble_button" />

    <TextView
        android:text="TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/folderImage"
        android:layout_toRightOf="@+id/folderImage"
        android:layout_toEndOf="@+id/folderImage"
        android:id="@+id/folderName" />

</RelativeLayout>
